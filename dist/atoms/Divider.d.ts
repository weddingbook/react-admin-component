import { CSSProperties } from 'react';
import './Divider.scss';
declare type Props = {
    style?: CSSProperties;
};
declare const Divider: ({ style }: Props) => JSX.Element;
export default Divider;
